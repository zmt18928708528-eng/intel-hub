{
  "name": "intel-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Gold / GPU / Mac mini intel adapters + CLI + tiny dashboard. Not a full product.",
  "scripts": {
    "intel": "node --experimental-strip-types src/cli.ts",
    "gold": "node --experimental-strip-types src/cli.ts gold",
    "gpu": "node --experimental-strip-types src/cli.ts gpu",
    "macmini": "node --experimental-strip-types src/cli.ts macmini",
    "all": "node --experimental-strip-types src/cli.ts all",
    "serve": "node --experimental-strip-types src/server.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
